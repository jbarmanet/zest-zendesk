"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAppPaths = void 0;
const path = require("path");
const fs = require("fs");
const util = require("util");
const access = util.promisify(fs.access);
const getAppPaths = (appDirectories) => {
    // set default as current dir
    if (appDirectories.length === 0)
        appDirectories.push('.');
    const appPaths = appDirectories.map((dirName) => path.resolve(dirName));
    appPaths.forEach((dirPath) => {
        access(dirPath).catch(err => {
            console.error(err.message);
        });
    });
    return appPaths;
};
exports.getAppPaths = getAppPaths;
