"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateAppPath = void 0;
const fs = require("fs");
const path = require("path");
const chalk = require("chalk");
const errors_1 = require("@oclif/core/lib/errors");
const validateAppPath = (appPath) => {
    if (!fs.existsSync(path.join(appPath, 'manifest.json'))) {
        throw new errors_1.CLIError(chalk.red(`Invalid app path: ${appPath}`));
    }
};
exports.validateAppPath = validateAppPath;
